package com.upsaclay.common.domain.usecase

class VerifyExistanceEmailUseCase {

    operator fun invoke(email : String) : Boolean {
        TODO("vérifie l'existance de l'email" +
                "renvoyer erreur du type forbidenException")
    }
}